name: evalgate
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - numpy
  - pandas
  - scikit-learn
  - xgboost
  - networkx
  - pip:
      - "mcp[cli]"
      - fastmcp
      - langchain-mcp-adapters
      - langgraph
      - langchain-ollama
      - mlflow
      - python-dotenv
